{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "bun run build",
    "installCommand": "bun i",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}
